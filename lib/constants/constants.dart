const String appBaseURL = "https://api.restful-api.dev";
const initialAmount = 500.0;

class PageRoutes {
  static const sendAmountPage = "/send_amount";
  static const transactionPage = "/history";
  static const home = "/";
}
